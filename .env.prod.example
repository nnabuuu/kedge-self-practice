# ==============================================
# KEDGE SELF-PRACTICE PLATFORM - PRODUCTION
# ==============================================

# Database Configuration
DB_PASSWORD=your_super_secure_db_password_here_change_me
POSTGRES_DB=kedge_practice
POSTGRES_USER=kedge_user
DB_PORT=5432

# Redis Configuration  
REDIS_PASSWORD=your_secure_redis_password_here_change_me
REDIS_PORT=6379
REDIS_DB=0

# JWT Configuration (MUST be at least 32 characters)
JWT_SECRET=your_super_secure_jwt_secret_key_here_minimum_32_characters_required

# Hasura Configuration
HASURA_ADMIN_SECRET=your_hasura_admin_secret_here_change_me
HASURA_PORT=28717

# OpenAI Configuration (Optional - for AI features)
OPENAI_API_KEY=sk-your_openai_api_key_here_if_using_ai_features

# Application Ports
API_PORT=8718
PRACTICE_PORT=5174
PARSER_PORT=5173

# Domain Configuration (Update with your actual domains)
API_DOMAIN=api.yourdomain.com
PRACTICE_DOMAIN=practice.yourdomain.com
PARSER_DOMAIN=parser.yourdomain.com

# Application Configuration
APP_VERSION=1.0.0
MAX_FILE_SIZE=52428800
MAX_FILE_SIZE_MB=50

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password

# Logging Configuration
LOG_LEVEL=info

# SSL Configuration (for Let's Encrypt)
LETSENCRYPT_EMAIL=admin@yourdomain.com

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=7
S3_BACKUP_BUCKET=kedge-backups

# Security Configuration
CORS_ORIGINS=https://practice.yourdomain.com,https://parser.yourdomain.com
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=15

# Performance Configuration
NODE_OPTIONS=--max-old-space-size=2048
WORKER_PROCESSES=2

# ==============================================
# QUICK SETUP NOTES:
# 1. Copy this file to .env.prod
# 2. Update all passwords and secrets
# 3. Update domain names
# 4. Run: docker-compose -f docker-compose.prod.yml up -d
# ==============================================