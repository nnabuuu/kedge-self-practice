# API Configuration
# Option 1: Use proxy to avoid CORS (recommended for development)
VITE_API_BASE_URL=/api
VITE_PROXY_TARGET=http://localhost:8718

# Option 2: Direct connection (requires CORS configuration on backend)
# VITE_API_BASE_URL=http://localhost:8718

# Quiz Parser URL for batch import
VITE_QUIZ_PARSER_URL=http://localhost:5173

# Development Settings
VITE_ENABLE_MOCK_API=false